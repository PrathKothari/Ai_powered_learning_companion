import React from "react";

const SectionHeading = ({ children }) => (
  <h2 className="text-xl font-bold text-gray-800 border-b border-indigo-300 pb-1 mb-4">
    {children}
  </h2>
);

export default SectionHeading;
